# CrypEval 密码安全参数估算器 — 学术报告

---

## 一、研究背景

随着后量子密码学（PQC）标准化进程的推进，格密码体制（如 LWE、CKKS）正逐步取代传统 RSA 和 ECC，成为下一代密码系统的核心。然而，为这些密码原语选择安全参数并非易事——它要求研究者同时理解格归约攻击（BKZ）、数域筛法（GNFS）、椭圆曲线离散对数（ECDLP）等多种数学困难假设，并能在安全性和性能之间做出权衡。

传统的参数选择通常依赖查阅静态表格（如 NIST SP 800-57 或 HE Standard），缺乏交互性和直观性。**CrypEval** 正是为填补这一空白而设计：一个交互式的、多原语支持的密码安全参数估算与可视化工具。用户通过调整维度 *n*、模数 *q*、噪声标准差 *σ* 等参数，可以实时观察到对应安全级别 *λ* 的变化，并通过趋势曲线理解参数之间的制约关系。

此外，对于对称密码和哈希函数，CrypEval 还提供了一个**多对手硬件暴力破解模拟器**，基于真实的硬件哈希率（CPU、GPU、ASIC 集群），直观展示不同密码长度和哈希算法（MD5、SHA-256、Argon2 等）在面对不同级别攻击者时的抗破解能力。

---

## 二、需求分析

### 2.1 功能需求

| 模块 | 功能 | 底层密码假设 |
|------|------|-------------|
| LWE 估算器 | 输入 (*n*, *q*, *σ*)，输出安全级别 λ | GapSVP → BDD → uSVP |
| CKKS / RLWE 估算器 | 输入环维度 *N*、系数模数 *Q*，估算 RLWE 安全性 | Ring-LWE → Ideal-SVP |
| RSA 估算器 | 输入模数长度（bit），输出 GNFS 安全性 | 整数分解 / GNFS |
| ECC 估算器 | 输入曲线位宽，输出经典安全性 + 量子漏洞提示 | ECDLP / Pollard's rho + Shor |
| 暴力破解模拟器 | 输入算法类型、密码长度、字符集，输出多硬件时间估算 | 穷举搜索 |
| 参数权衡曲线 | 维度/模数 vs 安全级别的动态趋势图 | 同 LWE / CKKS |

### 2.2 架构需求

- **前后端分离**：FastAPI 负责所有密码学计算，Streamlit 负责 UI 交互。前端不执行任何密码计算，所有估算通过 REST API 委托给后端。
- **多原语支持**：系统需在单一界面中支持格密码、传统公钥密码和对称密码的切换。
- **可复现性**：venv 虚拟环境隔离，`requirements.txt` 声明最小依赖，`.gitignore` 排除环境文件。

### 2.3 用户角色

- 密码学研究者：验证参数选择是否满足目标安全级别。
- 学生/教育者：通过交互式可视化理解密码学困难假设的量化关系。
- 安全工程师：评估企业系统在面对不同攻击者时的暴力破解风险。

---

## 三、实现过程

### 3.1 Agentic Workflow 开发流程

本项目完全采用 **Agentic Workflow（Claude Code）** 进行开发，共分为 8 个阶段：

1. **Phase 1 — 项目脚手架**：创建 `backend/`、`frontend/` 目录和 `requirements.txt`
2. **Phase 2 — LWE 核心引擎**：实现 δ → β → λ 的 Core-SVP 估算链（牛顿法/二分法求解 BKZ 块大小）
3. **Phase 3 — Streamlit 前端**：侧边栏参数滑块、指标卡片、可展开的密码假设说明
4. **Phase 4 — 多原语扩展**：新增 CKKS (RLWE)、RSA (GNFS) 两个估算器及对应 API 端点
5. **Phase 5 — ECC 支持**：椭圆曲线模块，Pollard's rho + Shor 量子漏洞警告
6. **Phase 6 — 参数权衡可视化**：`/evaluate/lwe/curve` 和 `/evaluate/ckks/curve` 端点 + 前端折线图
7. **Phase 7 — 多对手暴力破解模拟器**：5 种硬件配置 × 5 种哈希算法，emoji 标识的安全评级表格
8. **Phase 8 — 学术报告与演示文稿**：REPORT\_DRAFT.md 和 PPT\_OUTLINE.md

### 3.2 关键问题与解决方案

#### 问题 1：BKZ 公式求根的不稳定性

**现象**：LWE 估算器的 `_delta_to_bkz_block_size` 函数最初使用牛顿迭代法，但在 δ 较大（安全性弱）时发散振荡。

**原因**：BKZ 的 δ-β 关系公式是非单调的——函数 *f(β)* 在小 β 和大 β 处有两个根，牛顿法会收敛到错误的（低 β）根，或因为导数为零而发散。

**解决方案**：改用二分法（bisection），从函数最小值右侧开始搜索，确保找到物理上有意义的上根（大 β）。同时在公式中补充了被遗漏的 `ln(πβ)/β` 项，修正了 BKZ 根 Hermite 因子的精确表达式。

#### 问题 2：Python venv 隔离与 WSL2 依赖安装

**现象**：WSL2 环境中 `pip` 命令不存在，且 `python3 -m pip` 同样找不到 pip 模块。

**解决方案**：通过 `apt install python3-pip` 安装 pip，再通过 `python3 -m venv venv` 创建虚拟环境。验证通过编译检查（`py_compile`）在无 FastAPI 环境下确认代码语法正确。

#### 问题 3：WSL2 到 GitHub 的推送失败

**现象**：
- SSH：端口 22 被网络防火墙阻止（`kex_exchange_identification: Connection closed by remote host`）
- HTTPS：WSL2 中未安装 Git Credential Manager

**解决方案**：从 Windows 宿主机终端执行 `git push`，利用 Windows 端的 Git Credential Manager 完成身份验证，无需在 WSL 内额外配置。

---

## 四、工具对比分析：Claude Code vs 云端 AI 编程平台

### 4.1 对比维度

| 维度 | Claude Code (本地 Agentic) | 云端平台 (如"码上初发") |
|------|---------------------------|---------------------|
| **文件操作能力** | 直接读写本地文件系统，支持精确编辑（`Edit` 工具按字符串匹配替换） | 通常需上传/下载，大项目中操作粒度受限 |
| **命令行集成** | 原生 Bash 执行，可直接运行 `python3`、`git`、`uvicorn` | 依赖云端沙箱环境，网络和权限受限 |
| **版本控制** | 无缝对接 Git（commit、push、分支管理在对话中完成） | 通常需手动操作或额外的 CI 集成 |
| **迭代速度** | 即时反馈（修改→编译检查→测试在同一对话中完成） | 受网络延迟影响，每次修改需等待云端响应 |
| **领域深度** | 在密码学数学引擎开发中表现出色（BKZ 公式修正、GNFS 校准等） | 通用型 AI 可能在专业领域产生幻觉 |
| **上下文保持** | 全对话上下文可追溯，支持跨阶段复用（如 δ→β→λ 函数被 CKKS 复用） | 会话切换可能导致上下文丢失 |
| **安全性** | 代码和数据不离开本地环境 | 代码上传至第三方服务器 |

### 4.2 核心优势总结

Claude Code 在本项目中的最大优势体现在**复杂数学引擎的迭代调试**上。例如，BKZ 公式的求根问题需要在对话中反复测试不同 δ 值的 f(β) 行为、追踪牛顿法发散原因、最终切换到二分法——这一过程需要深入理解密码学数学细节，而 Claude Code 的本地执行 + 对话记忆使其能够像一位密码学研究员一样逐步分析和修正问题。

相比之下，传统云端 AI 编程平台更适合生成标准 CRUD 代码和前端 UI，但在需要领域专业知识的数学建模和算法修正方面，往往只能给出表面正确的代码，缺乏深度调试和推理能力。

---

## 五、代码运行说明及分析

### 5.1 项目结构

```
CrypEval/
├── backend/                       # FastAPI 后端（密码计算引擎）
│   ├── main.py                    # API 入口，路由分发 (7 个端点)
│   ├── lwe_estimator.py           # LWE: δ → β → λ 估算
│   ├── ckks_estimator.py          # CKKS / RLWE 安全估算
│   ├── rsa_estimator.py           # RSA GNFS 复杂度估算
│   ├── ecc_estimator.py           # ECC Pollard's rho + Shor 警告
│   └── bruteforce_estimator.py    # 多对手硬件暴力破解模拟
├── frontend/
│   └── app.py                     # Streamlit UI
├── requirements.txt               # 依赖声明 (fastapi, uvicorn, streamlit, numpy)
├── REPORT_DRAFT.md                # 学术报告
├── PPT_OUTLINE.md                 # 演示文稿大纲
└── README.md                      # 项目说明
```

### 5.2 后端架构分析

FastAPI 作为薄路由层，每个估算器是独立的纯 Python 模块：
- `lwe_estimator.py` — 核心函数 `_delta_to_bkz_block_size(delta)` 使用二分法求解 BKZ 块大小 β，`estimate_lwe_security(n, q, sigma)` 返回完整安全评估
- `ckks_estimator.py` — 复用 `lwe_estimator._delta_to_bkz_block_size`，针对环维度 N 和系数模数 Q 调整公式
- `rsa_estimator.py` — GNFS 子指数复杂度 *L<sub>N</sub>(1/3, (64/9)<sup>1/3</sup>)*，经 NIST/ECRYPT-II 校准
- `ecc_estimator.py` — Pollard's rho 经典攻击（*λ* = k/2）+ Shor 量子攻击（多项式时间）
- `bruteforce_estimator.py` — 5 个硬件配置 × 5 种哈希算法的穷举时间表

### 5.3 前端架构分析

Streamlit 采用声明式布局：
- **侧边栏**：原语选择器 → 动态参数滑块（随原语切换变化）
- **主区域**：指标卡片 → 安全评级 → 可展开说明 → 趋势曲线（LWE/CKKS）
- 前端通过 `requests` 库调用 `http://127.0.0.1:8000/evaluate/*` 端点，所有密码计算在服务端完成

### 5.4 启动命令

```bash
# 终端 1 — 后端
cd backend && uvicorn main:app --host 127.0.0.1 --port 8000

# 终端 2 — 前端
cd frontend && streamlit run app.py
```

---

## 六、使用流程

1. **启动系统**：分别在两个终端启动后端（FastAPI，端口 8000）和前端（Streamlit，端口 8501）
2. **选择原语**：在侧边栏下拉菜单中选择密码原语（LWE、CKKS、RSA、ECC 或 Brute-force）
3. **调节参数**：根据所选原语，调整对应的滑块（维度、模数、噪声、密码长度、字符集等）
4. **评估安全**：点击 "Evaluate Security" 按钮，前端向后端发送请求，获取安全估算结果
5. **查看结果**：
   - 指标卡片显示 *λ*、*δ*、*β*（或曲线位宽/GNFS 成本）
   - 颜色编码的安全评级（红色不安全 → 绿色强安全）
   - 可展开的密码学困难假设说明（含理论归约解释）
6. **探索权衡**（LWE/CKKS）：下方折线图自动加载参数扫描数据，展示维度/模数对安全性的影响趋势
7. **暴力破解评估**（Brute-force）：表格对比 5 种攻击者硬件配置下的破解时间，按 🔴/🟡/🟢 分级

---

## 七、总结反思

### 7.1 密码学知识收获

通过构建 CrypEval，我深入理解了以下可证明安全概念：

- **Core-SVP 模型**：格密码的安全性不是由密钥长度直接决定，而是通过 BKZ 格归约算法的块大小 β 与安全比特 λ 的线性关系（*λ* ≈ 0.292·β）来估算。这使我真正理解了"可证明安全"与"参数选择"之间的关系。
- **GNFS 复杂度**：RSA 的安全性并非随密钥长度线性增长，而是遵循子指数复杂度 *L<sub>N</sub>(1/3, 1.923)*。1024-bit 的 RSA 仅提供约 80 比特安全性，而 2048-bit 也仅 ~112 比特。
- **ECDLP 与量子漏洞**：ECC 的经典安全性很高（256-bit 曲线 → 128-bit 安全），但在 Shor 算法面前完全崩溃。这凸显了后量子密码迁移的紧迫性。
- **内存硬化密码**：Argon2 通过内存带宽瓶颈有效压制 GPU/ASIC 的并行优势，使暴力破解成本从 "秒级" 提升到 "宇宙年龄级"。

### 7.2 AI 辅助开发反思

- **效率提升显著**：从数学公式推导到前端 UI 的完整代码生成，8 个阶段在单次对话会话中完成，这在传统开发模式下需要数周。
- **关键价值在于审查**：AI 生成的密码学公式需要人工验证（如 BKZ 求根 bug 就是在手工验证 δ=1.004 时应得 ~128-bit 的结果偏离后发现的）。
- **Agentic Workflow 的优势**：每阶段代码变更后，Claude Code 自动进行语法检查和数学验证（在无 FastAPI 环境中也能通过 `py_compile` 和直接调用估算模块来验证逻辑正确性），这种即时反馈循环是传统 AI 代码生成所不具备的。
- **局限性**：AI 对密码学常数（如 GNFS 的 *o(1)* 校正项）的选择需要用户提供领域指导，自动生成的值可能与 NIST 标准有偏差。

---

*报告结束。CrypEval 项目为密码学教育与参数选择提供了实用的交互式工具，同时展示了 AI 辅助开发在密码工程领域的潜力与局限。*
