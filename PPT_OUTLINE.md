# CrypEval 演示文稿大纲 — 15 分钟学术汇报

---

## Slide 1：标题与项目简介 (1 min)

**标题**：CrypEval — 可证明安全参数估算器的设计与实现

**副标题**：基于 Agentic Workflow 的多原语密码安全交互式分析工具

**要点**：
- 课题背景：后量子密码标准化 → 参数选择成为关键挑战
- 项目目标：构建交互式、多原语、可视化的安全参数估算平台
- 技术栈：FastAPI（后端计算引擎）+ Streamlit（前端 UI）

---

## Slide 2：研究背景与痛点 (1.5 min)

**核心痛点**：
1. 格密码参数 (*n*, *q*, *σ*) 与安全级别 *λ* 之间的关系非直观——Δ δ = 0.001 的变化意味着 β 相差数百
2. 传统方法依赖静态表格（NIST、HE Standard），缺乏交互性
3. 多原语（LWE、RSA、ECC）的困难假设各不相同，难以横向比较
4. 暴力破解时间对密码长度的敏感性缺乏可视化工具体现

**举例**：
- 1024-bit RSA ≈ 80-bit 安全（已不安全）
- 256-bit ECC ≈ 128-bit 安全（经典），量子 = 0-bit
- CKKS N=32768, log₂Q=200 → ~108-bit，降 Q 可换取性能但安全性下降

---

## Slide 3：系统架构 (1.5 min)

**架构图**：

```
┌─────────────────────────────────────────────┐
│                Streamlit 前端               │
│  侧边栏(参数选择) │ 指标卡片 │ 折线图        │
│  requests.get() → http://127.0.0.1:8000     │
└──────────────────┬──────────────────────────┘
                   │ REST API
┌──────────────────▼──────────────────────────┐
│               FastAPI 后端                   │
│  /evaluate/lwe │ /ckks │ /rsa │ /ecc │ /bf │
│  /evaluate/lwe/curve  │ /ckks/curve         │
│                                              │
│  lwe_estimator.py  ← δ=(q/σ)^(1/n)          │
│  ckks_estimator.py ← RLWE 同 LWE pipeline    │
│  rsa_estimator.py  ← GNFS L_N(1/3, 1.923)   │
│  ecc_estimator.py  ← Pollard's rho + Shor    │
│  bruteforce_estimator.py ← 5 HW × 5 algos    │
└──────────────────────────────────────────────┘
```

**关键设计决策**：
- 前后端分离：前端不执行密码计算
- 纯 Python 数学引擎：不依赖外部密码库（如 SEAL），确保可审计性
- 模块化估计器：每个原语独立文件，共享 BKZ δ→β 转换函数

---

## Slide 4：核心功能展示 (1.5 min)

**五大多原语模块**：

| 原语 | 困难假设 | 攻击方法 | 安全公式 |
|------|---------|---------|---------|
| LWE | GapSVP | BKZ 格归约 | *λ* ≈ 0.292·β |
| CKKS/RLWE | Ideal-SVP | 典范嵌入 + BKZ | *λ* ≈ 0.292·β |
| RSA | 整数分解 | GNFS | *L<sub>N</sub>(1/3, 1.923)* |
| ECC | ECDLP | Pollard's rho | *λ* = k/2 |
| Brute-force | 穷举搜索 | 多硬件并行星号 | *T* = S / rate |

**交互特性**：
- 动态滑块：参数变化即时反映
- 颜色编码评级：🔴 不安全 → 🟢 强安全
- 可展开说明：每个原语均包含理论归约解释

---

## Slide 5：理论演示 — CKKS 安全权衡曲线 (2 min)

**演示内容**：

1. 选择 CKKS / RLWE 原语
2. 固定环维度 N=8192，σ=3.2
3. 展示 `st.line_chart` 曲线：
   - X 轴：log₂ Q（系数模数，10 → 800）
   - Y 轴：安全级别 λ（bits）
4. 观察：log₂Q=10 → 1124 bits；log₂Q=110 → 29 bits；log₂Q≥210 → 0 bits

**密码学解释**：
- *δ* = (Q/σ)<sup>1/N</sup>，Q 增大 → δ 偏离 1.0 → BKZ 所需 β 减小 → λ 降低
- 这就是 CKKS 方案必须在安全性和同态计算深度之间权衡的根本原因
- 箭头标注 "可证明安全区域" (λ > 128) 和 "不安全区域" (λ < 80)

**截图建议**：展示 Streamlit 折线图 + 标注

---

## Slide 6：实践演示 — 硬件暴力破解对比 (2 min)

**演示内容**：

| 场景 | 密码 | 算法 | 国家级别破解时间 |
|------|------|------|----------------|
| 弱密码 | 8 位字母数字 | MD5 | **2 秒** 🔴 |
| 弱密码 | 8 位字母数字 | SHA-256 | **1 秒** 🔴 |
| 弱密码 | 8 位字母数字 | NTLM | **< 1 秒** 🔴 |
| 中等 | 8 位字母数字 | WPA2 PBKDF2 | **25 天** 🟡 |
| 安全 | 8 位字母数字 | **Argon2** | **692 年** 🟢 |
| 强密码 | 12 位全 ASCII | Argon2 | **Unbreakable (>10⁵ 年)** 🟢 |

**关键洞察**：
- 快速哈希（MD5、SHA-256、NTLM）在 ASIC 集群面前不堪一击
- 内存硬件密码（Argon2）通过 VRAM 带宽瓶颈有效压制 GPU/ASIC 并行优势
- 即使是国家级别攻击者，面对 Argon2 + 长密码也束手无策

**截图建议**：Streamlit 暴力破解表格（含 🔴🟡🟢 emoji）

---

## Slide 7：开发过程与问题解决 (2 min)

**Agentic Workflow 8 阶段开发**：

```
脚手架 → LWE 核心 → Streamlit UI → 多原语扩展 → ECC
→ 可视化 → 暴力破解 → 报告/PPT
```

**三个关键 Bug 及解决方案**：

1. **BKZ 公式求根发散**
   - 问题：牛顿法在 δ 大时振荡，收敛到错误根
   - 根因：BKZ δ-β 函数非单调（双根），且公式遗漏 `ln(πβ)/β` 项
   - 解决：补全公式 + 从函数最小值右侧二分搜索

2. **WSL2 Git 推送失败**
   - 问题：SSH (端口 22 被阻) + HTTPS (无 Credential Manager)
   - 解决：从 Windows 宿主机终端推送

3. **venv 依赖安装**
   - 问题：WSL2 默认无 pip
   - 解决：`apt install python3-pip` + `python3 -m venv venv`

---

## Slide 8：AI 工作流对比 (1.5 min)

**Claude Code (Agentic) vs 云端 AI 编程平台（码上初发）**：

| 维度 | Claude Code | 码上初发 |
|------|------------|---------|
| 文件操作 | 本地精确编辑 | 云端受限 |
| 命令执行 | 原生 Bash | 沙箱受限 |
| Git 集成 | 对话中直接 commit/push | 手动或 CI |
| 领域深度 | 密码数学可深度调试 | 易产生幻觉 |
| 迭代速度 | 即时反馈（修改→验证→测试） | 网络延迟 |
| 数据安全 | 代码不离开本地 ✓ | 上传至第三方服务器 |

**结论**：对于密码工程等需要领域专业知识的项目，Agentic Workflow 的核心优势在于**本地执行 + 完整上下文 + 即时验证**的闭环。

---

## Slide 9：结论与展望 (1.5 min)

**研究成果**：
- 成功构建了覆盖 5 种密码原语的交互式安全参数估算平台
- 实现了 δ → β → λ 的 Core-SVP 经典格归约分析
- 提供了 GNFS、ECDLP、暴力破解等多种攻击模型的定量估算
- 通过模块化架构使新增密码原语只需添加一个 Python 文件 + 一个端点

**个人收获**：
- 深入理解了可证明安全（Provable Security）与参数选择的数学关系
- 掌握了 BKZ 块大小估算、GNFS 子指数复杂度等密码分析技术
- 实践了 AI 辅助开发的完整工作流

**未来展望**：
- 加入量子安全级别的 BKZ 估算（Core-SVP quantum: *λ* ≈ 0.265·β）
- 支持更多同态加密方案（BFV、TFHE）
- 集成 NIST PQC 标准化算法的完整参数集（Kyber、Dilithium）
- 添加散点热力图（n vs q vs λ 三维可视化）

---

*汇报结束，感谢聆听。欢迎提问。*
